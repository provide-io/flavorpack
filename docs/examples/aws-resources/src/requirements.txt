# AWS Resources Provider Requirements
# Production-ready dependencies for Flavor AWS provider example

# AWS SDK for Python - Core AWS service integration
boto3>=1.26.0,<2.0.0
botocore>=1.29.0,<2.0.0

# HTTP requests and urllib3 (pinned for security)
requests>=2.28.0,<3.0.0
urllib3>=1.26.12,<2.0.0

# Data validation and serialization
pydantic>=1.10.0,<3.0.0
pydantic[email]>=1.10.0,<3.0.0

# JSON handling and data manipulation
orjson>=3.8.0,<4.0.0

# Date and time handling
python-dateutil>=2.8.2,<3.0.0

# Cryptography for additional security features
cryptography>=39.0.0,<42.0.0

# Logging and structured output
structlog>=22.3.0,<24.0.0
colorama>=0.4.6,<1.0.0

# Configuration management
python-dotenv>=0.19.0,<2.0.0

# Retry logic and backoff
tenacity>=8.1.0,<9.0.0

# JSON schema validation
jsonschema>=4.17.0,<5.0.0

# Type checking support (development)
types-requests>=2.28.0
types-python-dateutil>=2.8.0

# Optional: Performance improvements
# uvloop>=0.17.0; sys_platform != "win32"  # Async I/O performance
# orjson>=3.8.0  # Faster JSON processing (already included above)

# Security note: Keep these dependencies updated regularly
# Run: pip-audit to check for known vulnerabilities
# Use: dependabot or similar for automated updates

# Package size optimization:
# These dependencies are carefully chosen to balance functionality and size
# Total estimated size: ~15-20MB for dependencies + ~20-30MB Python runtime
# Final Flavor package size: ~40-60MB depending on features used